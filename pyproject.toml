[project]
name = "ai-data-shard-lab"
version = "0.1.0"
description = "Local HTTP worker experiments for reliable AI text-data processing"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint.per-file-ignores]
"scripts/*.py" = ["E402"]
